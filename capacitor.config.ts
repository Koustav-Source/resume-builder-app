import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.koustav.resumebuilder',
  appName: 'ResumeBuilder',
  webDir: 'dist'
};

export default config;
